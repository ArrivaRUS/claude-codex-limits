# build output
dist/
*.dmg

# macOS
.DS_Store

# editor / tooling
.claude/
*.swiftmodule
*.o

# runtime data (never commit — may contain credentials/usage)
cache.json
app.lock
build.log
backups/

# local time ledger (Юрка time-tracking — не коммитить)
*.local.md
