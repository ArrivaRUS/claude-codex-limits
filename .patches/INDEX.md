# Индекс уроков — 2026_06_UsageLimits

> Строка = правило. Читается на старте сессии вместо всех файлов (правило HANDOFF §5, 2026-09-02).
> Отдельный урок открывай, только если он относится к текущей задаче. Стабильное правило → в устав агента/скилл, урок помечается «встроено».

| Файл | Тема | ПРАВИЛО НА БУДУЩЕЕ |
|---|---|---|
| `001-silent-exit-fresh-mac-and-gatekeeper.md` | 001 — Тихий выход на чистом маке + тупик Gatekeeper на Sequoia/Tahoe | Любой путь к файлу в домашней папке = сначала создай директорию. Каждый `open()`/`write` в `DATA_DIR`-подобные пути — только после `createDirectory(withIntermediateDirectories: true)`. `try?` на записи — запах: ошибка пр… |
| `002-claude-cli-path-not-set.md` | 002 — Установщик Claude Code CLI не прописывает PATH | Инструкция для пользователя обязана покрывать не только happy-path, но и первый реальный грабель после каждого шага («установил, а команда не найдена»). Источник таких граблей — живые сессии поддержки: каждый вопрос поль… |
| `003-codesign-vs-cloud-synced-dist.md` | 003 — codesign нестабилен, когда dist/ лежит на облачном томе | Артефакты сборки, требующие подписи, не должны жить в папке под облачным синком — либо staged-копия на локальном томе (как сейчас), либо вынести `dist/` из синка. При странных «плавающих» ошибках codesign первым делом пр… |
| `004-menubar-appearance-not-system-theme.md` | 004 — Вид строки меню ≠ системная тема (невидимые цифры в трее) | Для отрисовки в строке меню тему всегда брать у самой кнопки статус-айтема, не у приложения/системы: menu bar может быть тёмной при светлой теме (обои). Превью не заменяет реальный экран. В превью `dark:` задавался явно,… |
